# skills

Claude Code commands that work off the same vault as the scheduler. Each is a single prompt file. Drop one in your Claude Code commands directory, or run it as a slash command.

- **dayplan** - build today's plan from the calendar and the backlog. Read only.
- **capture** - write what a working session produced into the right notes.

More to follow.
